# CVE Submission Tracker

This repository stores GitHub issues for vulnerability submission drafts and downloadable package archives referenced by those issues.

## Current attachments

- Issues `#1` to `#5` each have a matching zip package under `attachments/`.
- The issue body contains the main report content.
- A follow-up issue comment provides the corresponding package download link.